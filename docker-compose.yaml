services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: pari
      POSTGRES_PASSWORD: pari_dev_pw
      POSTGRES_DB: pari
    ports:
      - "5432:5432"
    volumes:
      - pari_pgdata:/var/lib/postgresql/data
volumes:
  pari_pgdata: